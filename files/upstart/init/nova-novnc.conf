description "Nova noVNC"
author "Yun Mao <yunmao@gmail.com>"

start on (filesystems and net-device-up IFACE!=lo)
stop on runlevel [016]

chdir %DIR%
pre-start script
        mkdir -p /var/log/nova
        chown %USER%:root /var/log/nova/
end script

#respawn

exec su -c "cd %DIR% && %DIR%/utils/nova-wsproxy.py --flagfile=%NOVA_DIR%/bin/nova.conf --web . 6080 >> /var/log/nova/nova-novnc.log" %USER%
